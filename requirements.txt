# Hugging Face Spaces (and full local demo): pip install -r requirements.txt
# Lighter local UI only: pip install -r requirements-demo.txt
gradio>=4.44.0,<5
huggingface_hub>=0.23.0
librosa>=0.10.0
matplotlib>=3.8.0
numpy>=1.26.0,<3
python-dotenv>=1.0.0
scipy>=1.11.0
soundfile>=0.12.0
# HF Spaces builder uses Python 3.13; TF<2.20 has no cp313 wheels on Linux.
tensorflow-cpu>=2.20.0,<2.22
